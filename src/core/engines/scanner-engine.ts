import { Html5QrcodeAdapter } from "../../libraries/html5qrCode";

// export const scannerEngine: IScannerEngine = new Html5QrcodeAdapter();
// can directly go to file where it is implementd
export const scannerEngine = new Html5QrcodeAdapter();
